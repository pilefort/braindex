// Package scantest はテストから「見つかるが読めないファイル」「列挙できないディレクトリ」を作る道具。
//
// 走査(scan)と索引生成(catalog)は読めなかった範囲を記録するが、その状態を再現する手段が OS で違う。
// Unix は権限を 000 にすれば済むが、Windows では権限が効かないので、排他で開いたままにして
// 他の open・列挙を共有違反で失敗させる(2026-09-06 実測: Stat は通り、ReadFile と ReadDir が失敗し、
// filepath.WalkDir はそのディレクトリについて 2 回目の呼び出しでエラーを渡す)。
// どちらもテスト終了時に元へ戻す。バイナリからは参照しない。
package scantest

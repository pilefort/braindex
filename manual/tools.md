# braindex approvals / answer / explain / verify / scope — 判断・HTML・照合・走査対象

[← README](../README.md) ／ [手引きの目次](README.md)

## braindex verify session — 発話と実行記録の照合

アシスタントが「テストが通りました」「コミットしました」などと書いた文について、
同じセッション内の、その発話より前の Bash 呼び出しを照合する。本体とサブエージェントの
発話・実行を含む。ノートや ISSUE は読まず、外部への通信もしない。

```sh
braindex verify session sample-session
braindex verify session ./logs/sample-session.jsonl
braindex verify -json session ./logs/sample-session.jsonl
```

ID は `sessions.DefaultDir` が返す既定の置き場（`~/.claude/projects`）の各プロジェクトから探す。
同じ ID が複数あれば `ERROR`。パスは `.jsonl` ファイルを指定する。`-json` は `session` より前に置く。

### 固定の辞書

括弧内の助詞は省略できる。英語の発話は大文字・小文字を区別しない。
コマンドは表の文字列が Bash の `command` に含まれるかで照合する。

| 種 | 発話に含まれる表現 | 対応するコマンド |
|---|---|---|
| test | テスト（が／を／は）通・パス・成功、test/tests pass/passed | `go test`、`npm test`、`npm run test`、`pnpm test`、`yarn test`、`pytest`、`cargo test`、`make test` |
| build | ビルド（が／は）通・成功、vet（が）通 | `go build`、`go vet`、`npm run build`、`cargo build` |
| commit | コミットした／しました／済み | `git commit` |
| push | pushした／しました／済み、プッシュした／しました | `git push`、`gh pr create` |

本文を句点・終止符（`。！？.!?`）と改行で区切る。バッククォートまたはチルダのフェンス、
4 空白またはタブで字下げしたコード行、`>` で始まる引用行は除く。
同じ文に `ない`、`なかった`、`ません`、`未`、`ず`、`これから`、`予定`、`つもり`、
英単語 `no/not/never/will/plan/planned` または `n't` があれば、その文は拾わない。
同じ文で同じ種が繰り返されても 1 件とし、異なる種はそれぞれ出す。

### 判定と終了コード

出力は既存の `Kind Target Status Detail` のタブ区切り、または `-json` の JSON 配列。
`Kind` は `session:test` など、`Target` は主張の文（改行・タブ等は空白）。
対応する実行の詳細には、呼び出し時刻、`subagent=true/false`、空白を整えたコマンドの先頭 60 文字を出す。

| 判定 | 意味 | 終了コード |
|---|---|---|
| FOUND | 発話より前の最後の対応する呼び出しが `IsError=false` | 0 |
| FAILED | 最後の対応する呼び出しが `IsError=true` | 2 |
| NOT FOUND | 発話より前に対応する呼び出しがない、または発話の時刻がない | 2 |
| ERROR | セッションを読めない、対象ログに読み取り警告がある、ID が一意でない | 1 |

混在時は `ERROR` を優先する。主張がなければ `session`・`FOUND`・「照合対象の主張がない」を
1 行出して成功する（JSON でも結果 1 件）。同時刻の実行は「発話より前」に含めない。
対応する呼び出し同士が同時刻なら、読み取り順の後を採用する（本体の後にサブエージェント）。

### 限界

- 辞書にない言い回しは拾わない。否定語の部分一致や単純な文区切りによって、見逃しも起こる。
- 時刻のない行は照合できない。時刻のない呼び出しは候補から外し、時刻のない主張には理由を表示する。
- コマンドの部分一致であり、シェルの構文や対象リポジトリは解析しない。コメントや `echo` 内の文字列も一致する。
- `IsError` だけで判定する。`HasResult=false` でも `IsError=false` なら `FOUND` になるが、詳細に
  「結果の記録がないため成否未確認」を添える。結果の受信時刻は読み取り API にないため、発話までに完了したかは判定できない。
- 既存の `sessions.Dir` は人間の発話がないログを返さないため、そのログは `ERROR` になる。
  単一ファイルを読む公開 API がないため、パス指定でも親の置き場を読み、対象だけを取り出す。
  同じ置き場にログが多い場合は時間が掛かる。本文ブロックが束ねられた発話には、既存 API の発話時刻を使う。

## braindex approvals — 判断待ちのフォーム

`work/APPROVALS.md` の判断待ち（1 項目 1 判断・5 欄「決めたいこと／なぜ今決めるか／選択肢／私の案／決めないとどうなるか」）を
ブラウザのフォームにして聞き、答えを記録する。受け口は 127.0.0.1 の空きポートだけで、回答を 1 回受けたら終わる（常駐しない）。外へは何も送らない。

```sh
braindex approvals serve -apply   # フォームを開いて回答を待ち、そのまま反映する
braindex approvals status         # 件数・記載漏れ・未反映の回答（書き込みなし）
braindex approvals apply          # 受けた回答を反映する（聞くのと分けたいとき）
braindex approvals hook           # 停止フックから呼ぶ。残っていれば serve を切り離して起動する
braindex approvals wait           # hook が開いたフォームの回答を待ち、届いたら要約を出して終わる
```

選んだ項目は `docs/decisions.md` に 3 段（結論 → 理由 → 根拠）で追記して `APPROVALS.md` から消し、保留は項目を残して
「**保留（日付）:**」を付ける。反映した回答 JSON は、反映の結果（決定・保留の件数と警告）を書き足して `.applied.json` に移すので、2 回反映されない。
フラグ: `-file`（判断待ちのファイル）`-dir`（回答 JSON の置き場。既定は OS の一時ディレクトリの `braindex-approvals`）
`-config`（設定ファイル。既定はカレントの `braindex.json`。無くてもよい）`-timeout 秒`（0 で無期限）`-no-open` `-apply` `-decisions` `-date` `-reply`。
回答 JSON とその置き場は、共有 `/tmp` を持つ環境で同じマシンの他ユーザーから読めないよう `0600`／`0700` に絞って書く（Unix 系のみ。
Windows は POSIX の権限ビットをほぼ持たない）。

### 開き忘れを拾う（`hook`）

積んだのにフォームを開かず、文章で「判断待ちが残っています」と書くだけで終わってしまうことがある。
`hook` はそれを機械で拾う。エディタの**停止フック**（アシスタントの発話が終わるたびに走る仕掛け）から呼ぶと、
判断待ちが残っているときだけ `approvals serve -apply` を切り離して起動する。

```json
{ "hooks": { "Stop": [ { "hooks": [ { "type": "command", "command": "braindex approvals hook" } ] } ] } }
```

- stdin の JSON から `cwd`（判断待ちを探す場所）と `stop_hook_active`（既に指示で続きを書いている印）を読む。
  渡されなければカレントで代用する。`-file` を明示すればそちらが勝つ
- **同じ内容では一度しか開かない。** 開いた印は回答 JSON と同じ一時置き場に `hook-<id>.json` として置く。
  回答が入れば `APPROVALS.md` が変わるので、次の判断待ちではまた開く
- 起動したら stdout に `{"systemMessage":…,"decision":"block","reason":…}` を返し、アシスタントに
  「何を判断してほしいかを利用者に伝える」よう促す。`stop_hook_active` が真のときは促さない（往復が終わらなくなる）
- **促し文には、回答を待つ `approvals wait` の起動も入る**（`-file` と、既定以外なら `-dir` を付けたコマンド行。
  パスは Windows でも `/` 区切りで出す。Git Bash では引用符の外の `\` が消えるため）。
  フォームは切り離して動くので、回答が届いてもアシスタントには伝わらない。`wait` をバックグラウンドで起動させておき、
  その終了を「回答が届いた」知らせにする
- **終了コードは常に 0。** 判断待ちが無い・ファイルが無い・起動できない、のいずれでも会話を止めない
- フラグ: `-timeout 秒`（起動するフォームが待つ秒数。既定 3600。放置されたフォームを畳んでプロセスを残さない）
  `-reason`（促し文の上書き。利用側の規約に合わせたいとき。上書きした文にも `wait` の起動を書く）`-no-open`（ブラウザを開かない。動作確認・画面の無い環境）
  ／`-file` `-dir` `-config` は他のサブコマンドと同じ

### 回答を待つ（`wait`）

`hook` が開いたフォームの回答を待ち、届いたら要約を出して終わる。アシスタントがバックグラウンドで起動し、終わったことを知らせとして受け取る。

- 待つのは、`hook` がフォームを開いた時刻（`hook-<id>.json` の `opened`）以降に届いた回答。開いてから `wait` を起動するまでに
  答えられても拾う。印が無ければ、起動した時刻以降の回答を待つ
- 知らせた回答の受信時刻は印に `reported` として残し、同じ回答では 2 回終わらない
- `serve -apply` が反映して `.applied.json` に移し、書き足した反映の結果に警告が無ければ終了コード 0。
  警告がある（題が食い違って項目が見つからなかった等）か、`.reply.json` のまま残った（反映に失敗）ときは 2 を返す。
  `apply` は未反映の項目があっても回答を移すので、移ったかどうかでは判断しない
- フラグ: `-timeout 秒`（既定 3600。`hook` が開くフォームの待ち時間と同じ。0 で無期限）`-interval 秒`（見に行く間隔。既定 1）
  ／`-file` `-dir` `-config` は他のサブコマンドと同じ

設定（`braindex.json` の `approvals` 節。節ごと省略してよく、設定ファイルが無くても動く）:

```json
{ "approvals": { "file": "work/APPROVALS.md", "decisions": "docs/decisions.md", "timeout_sec": 0 } }
```

- `file` — 判断待ちのファイル。既定 `work/APPROVALS.md`
- `decisions` — 決定の追記先。既定 `docs/decisions.md`
- `timeout_sec` — `serve` が回答を待つ秒数。既定 `0`（無期限）。負の値は設定の誤りとして止める

相対パスは **`braindex.json` のある場所**からの相対で解く（コマンドを打ったカレントからではない）。
優先順位は **フラグ > 設定 > 既定** で、`-file` などを明示したときはフラグが勝つ。
設定ファイルは全体を読むので、`news` など**別の節にタイプミスがあると `approvals` も終了コード 1 で止まる**（他のコマンドと同じ挙動）。
設定ファイルが既定の置き場に無いのは正常で、そのときはフラグと既定だけで動く。`-config` で指定したのに無いときだけ失敗する。
終了コード: `serve` 0 回答あり／3 時間切れ、`apply` 0 反映した・回答なし／2 反映できなかった項目がある、`status` 0 ／2 記載漏れか未反映の回答あり、`wait` 0 回答が反映された／2 回答は届いたが未反映／3 時間切れ。いずれも 1 は失敗。

## braindex answer — 回答の HTML 化

`braindex answer <md>` は Markdown を自己完結の HTML（外部の JS・CSS を参照しない）にして書き、既定ブラウザで開く。
出力先の既定は一時置き場で、実行のたびに `-ttl-days`（既定 14）より古いものを消す。
**HTML は読むための一時物**なので、残す価値のある内容は `.md` を `docs/notes/` に置いてから渡す（置き場所が寿命を表す）。
リンクの `href` に出すのは `http(s)` と、スキームを持たないもの（相対パス・`#見出し`）だけ。`javascript:` などは文字として残す。
画像とリンクの相対パス（`![図](./fig.svg)` など）は、**渡した `.md` の置き場所**からの絶対パス（`file://`）にして出す。
HTML は一時置き場に書かれ `.md` と同じ場所に無いので、相対のままでは開けない。`#見出し` と `http(s)` はそのまま。

```sh
braindex answer note.md                  # HTML にして開く
braindex answer -no-open note.md         # 書くだけ
braindex answer -out out.html note.md    # 出力先を指定する
braindex answer -dir                     # 一時置き場の場所を表示して終わる
braindex answer -purge                   # 一時置き場の中を今すぐ全部消す
```

フラグ: `-out` `-no-open` `-dir` `-purge` `-ttl-days`（0 で消さない）`-append` `-q`。フラグは `<md>` より前に置く。終了コード: 0 成功／1 失敗。

### 同じ話題に重ねる（`-append`）

`-append <話題>` を付けると、一時置き場の `<話題>.md` を**スレッド**として扱い、その先頭に今回の回答を足す。
`-q` にはユーザーの質問を逐語で渡す（要約しない。長ければ先頭 1〜2 文）。エントリの見出しになる。

```sh
braindex answer -append 索引の設計 -q "走査の順番は決まってる？" ans.md
braindex answer -append 索引の設計 -q "root と notes_dirs はどっちが先？" ans2.md
```

- 新しい回答が**上**に積まれ、古いものは下に残る。回答本文の `##` 見出しでエントリが割れることはない
  （境界は本文に現れない HTML コメントのマーカーで持つ）
- **畳むか開くかはあなたが決める。** 畳んだエントリは次に開いたときも畳まれたまま、開いたものは開いたまま出る。
  読み終わったエントリを自分で畳んでいけば、次からは新しいものだけが開いた状態になる。
  一度も開閉していないエントリには「新着」の印が付く。右上の「全部開く／全部畳む」でまとめて切り替えられる
  （開閉の状態はブラウザの localStorage に、ファイル名とエントリごとに持つ）
- 常駐サーバを置かない代わりに、スレッドの HTML は **12 秒ごとに自分を読み直す**。隠れているタブと文字を選択中は読み直さず、
  スクロール位置と開閉は元に戻す。うるさければ右上の「自動更新」で止められる（1 枚ものの HTML には入らない）
- 1 回目の回答の `# 見出し` がスレッドの題名になる。2 回目以降の `# 見出し` はエントリの中には残さない
- スレッドの `.md` も一時置き場に置くので `-ttl-days` で消える。**残す価値のある内容は `docs/notes/` に別途書く**
- 話題名は一時置き場の中のファイル名なので、パス区切り（`/` `\`）や `:` `*` `?` `"` `<` `>` `|` は使えない

## braindex explain — 解説の HTML 化

`braindex explain <md>` は論文・記事の**解説**の Markdown を、目次と図を備えた自己完結 HTML にして開く。
置き場所・TTL・開き方は `answer` と同じで、**HTML は読むための一時物**。正本の `.md` と `.svg` は `docs/notes/` に置く。

取得も解説の生成もしない。文章と図は書き手が `.md` と `.svg` に書き、このコマンドは読みやすい形に組み直すところだけを担う。
LLM を呼ばないので、**同じ入力からは同じ HTML が出る**（決定 2026-09-12 → 下の「決めたこと」）。

```sh
braindex explain kaisetsu.md                  # HTML にして開く
braindex explain -no-open kaisetsu.md         # 書くだけ
braindex explain -out out.html kaisetsu.md    # 出力先を指定する
```

フラグ: `-out` `-no-open` `-ttl-days`（0 で消さない）。フラグは `<md>` より前に置く。
終了コード: 0 成功／1 失敗（図が見つからない等、本文に印を出した問題を含む。HTML は書いて開いたうえで 1 を返す）。

### md の書き方

- **図**: `![図1: 目次を検索単位にする](fig1.svg)` のように、行まるごとを画像にする。`.svg` は md からの相対パス。
  中身を HTML に埋め込むので、図の入った 1 枚が残る。alt の「図N: 説明」がそのまま `<figcaption>` になる。
  図は別ファイルに置く（決定 2026-09-12 → 下の「決めたこと」。md に直接 `<svg>` を書くと本文が図で埋まり、読み返す手間が増える）
- **見出し**: `##` が節、`###` が小節。目次はこの 2 段から作る。広い画面では左に固定され、狭い画面では本文の先頭に畳まれる。
  id は出現順の通し番号（`s1`・`s1-1`）なので、同じ見出しが 2 つあっても飛び先がずれない
- **出典**: 本文中のリンクをそのまま使う（`http(s)` 以外は `href` に出さない）
- **段落の改行**: 文の途中で折り返した行は、次の行と続けて 1 つの段落にする。
  **文の終わり（`。！？：`）で終わる行の後ろだけ改行を残す**ので、「結論:／理由:／根拠:」のように
  1 行 1 文で書いた箇所はそのまま 3 行で出る。どこでも改行したいときは行末に空白を 2 つ置く。
  長い解説は 1 段落を 100 文字くらいで折り返して書くので、全部を改行にすると出来上がりの行長（40 文字）と
  合わず、文の途中で不自然に切れる。`answer` はこの扱いをせず、今までどおり 1 行 1 改行で出す

### 図の色

**図の色は本文に合わせる。** `<svg class="bxfig">` にして、色は `fill="var(--fg)"` のように名前で書く
（値は書かない）。明るい配色と暗い配色の切り替えに、図もついてくる。

| 名前 | 何の色か |
|---|---|
| `--fg` / `--fg2` | 図の中の文字／補足の文字 |
| `--mono` | 等幅の文字（ファイル名・コマンド） |
| `--edge` / `--groove` / `--surf` | 線・矢印／目盛りの溝／囲みの地 |
| `--c1`〜`--c4` | 系列の色（短い見出しの文字にも使える濃さ） |
| `--s1`〜`--s4` | 系列の面（`--c1`〜`--c4` を薄く敷いたもの） |

系列の色が 4 つなのは、本文の棒グラフ（5 色）と用途が違うため。図では系列の色を文字にも使うので
下限が 4.5 になり、5 色のままでは青と赤紫を濃くしたときに色覚の型によって見分けにくくなる
（決定 2026-09-12）。5 つ目の系列が要る図は、形か線種で分ける。

色の値を図の側に書くと、本文と食い違ったまま出る。2026-09-12 に、暗い配色を前提に描いた図を
明るい本文へ入れて、文字と背景の比が 1.13 になった（読める下限は 4.5）。値は `explain` の側に
1 か所だけ置いてあり、`internal/explain/figpalette_test.go` が明暗それぞれの比を測る。

埋め込む `.svg` からは `<script>` と `on...` で始まる属性を落とす。`<style>` と `<animate>` は残すので、
図のアニメーションはこの 2 つで書く。図が見つからないときは、その場所に「図 fig1.svg が無い」と出して 1 を返す
（HTML は書けているので、どこが欠けたかを見て直せる）。

**色を当てたい文字に class を付けない。** `<text class="t" fill="var(--c1)">` のように書いても、
図の `<style>` に `.bxfig .t{fill:var(--fg)}` があればそちらが勝ち、`fill` 属性で当てた色は出ない
（CSS のプロパティ指定が SVG の presentation attribute より強いため。実測 2026-09-12）。系列の色を
文字に出したいときは、その要素に色を決める class を付けないか、class 側で `fill` を上書きしない。

### 数値のグラフ（表から描く）

棒と折れ線は、**表の数値から機械が描く**（決定 2026-09-12 → 下の「決めたこと」）。手で描くと棒の長さを目分量で決めることになり、
本文の数値と食い違っても読み返して気づけない。表から目盛りと長さを計算すれば、その食い違いは起きない。
**元の表はグラフの下に残す**ので、数字そのものも読める。

指定は表の直前の行に置く。HTML コメントなので GitHub でも本文に出ない（`explain` は読み取ったあとこの行を捨てる。
`answer` は対応しないので、そちらでは文字として出る）。

```
<!-- graph: bar x=手法 y=Recall@1,Recall@3 unit=% -->
| 手法 | Recall@1 | Recall@3 |
|---|---|---|
| BM25 | 41.2 | 58.9 |
| STAIR | 55.0 | 71.3 |
```

- `graph:` は `bar` か `line`。指定の無い表はそのまま表として出る
- `x=` は横軸にする列名（既定は 1 列目）。`y=` は描く列名をカンマ区切り
  （既定は x 以外の数値の列すべて。空でないセルの過半数が数値として読める列を数値の列とみなす）
- `unit=` は目盛りに付ける単位（省略できる）
- 桁区切りの `,` と末尾の `%` は落として読む。数値にならないセルは欠測として飛ばし、
  数値がひとつも無い行は描かない
- 色は固定の 5 色（色相は Okabe-Ito の並び）。**明るい配色と暗い配色で別の値**を使い、背景とのコントラスト比は
  どちらでも 3.0 以上にする。系列が 6 つ以上になると、折れ線は線種でも分かれる
- 棒は読み込み時に伸びる。`prefers-reduced-motion` の環境では動かさない
- 目盛りは必ず 0 を含む（途中から始まる軸で差を大きく見せない）

描けない指定（`graph: pie`・無い列名・表が続いていない）は、その旨を警告に出して**表だけ**を出し、終了コード 1 を返す。


## braindex verify — 実在の照合

ノートに書いた GitHub リポ・arXiv 論文・URL・逐語引用を、一次ソースへの GET で照合する（こちらから本文は送らない）。

```sh
braindex verify github pilefort/braindex
braindex verify arxiv 2608.26263
braindex verify url https://go.dev/blog/
braindex verify quote https://example.com/a "引用したい文を二十四字以上そのまま書く"
braindex verify -json github pilefort/braindex   # フラグは種別より前に置く
```

出力は 1 件 1 行（種別・対象・判定・実測のタブ区切り。判定は `FOUND`／`NOT FOUND`／`ERROR`。`-json` で配列）。
`github` は実在・スター数・作成日、`arxiv` は ID の実在（実測の列にタイトル）、`url` は HTTP 200 か、`quote` は本文（タグ除去・空白正規化）に引用が実在するか。
`quote` は空白の揺れだけ許し、24 字未満の引用は ERROR になる。`GITHUB_TOKEN` があれば GitHub API の認証に使う（任意・レート制限対策）。
`quote` の出典ページは本文 8 MiB で打ち切って取得する。打ち切りが起きたページで見つからなかった引用は、
`NOT FOUND`（無いと確定）ではなく `ERROR`（打ち切りの先は見ていないので判定できない）で返る。
終了コード: 0 全件 FOUND／2 NOT FOUND あり／1 失敗（ERROR あり・引数の誤り）。

## braindex scope — 矛盾検査の走査対象

複数ノートをまたぐ相互矛盾・陳腐化を探すとき、索引から走査対象を列挙・絞り込み・chunk 分割して出す。**矛盾の判定はしない**
（chunk ごとに実ファイルを全文読み比べ、反証で偽陽性を落とす手順は hub に入るスキル `contradiction-scan`）。

```sh
braindex scope -topic 長さ         # タイトル・要旨・パスに語を含む行(大小無視)
braindex scope -topic 長さ -repo alpha # 両方の条件を満たす行だけ
braindex scope -repo alpha         # その見出し(リポ名)の行だけ
braindex scope -full -size 20      # 全件を 20 件ずつの chunk に分ける
braindex scope -dir docs/notes     # 索引を使わず、ディレクトリ配下の *.md を列挙する
```

出力のパスは、どちらのモードでもそのまま開ける形で出る——索引を使うときは索引の行と同じ `root` 相対、
`-dir` のときは渡したディレクトリと結合した形。`-dir` の列挙は `archive` セグメントと `.` で始まるディレクトリの
配下を対象にしない（起点として直接渡したときだけは中を見る）。`archive` の扱いは索引と同じで、
`.` で始まるディレクトリも索引と同じく置き場の中まで除く。
`-topic` と `-repo` は併用でき、結果の `mode` は `topic:<語>+repo:<名>` になる。
`-dir` と `-full` はそれぞれ単独で指定する。他の絞り込みとの併用は終了コード 1。
フラグ: `-topic` `-repo` `-dir` `-full` `-size N`（既定 12）`-json`（`mode`・`n_entries`・`chunks`）`-catalog` `-config`。
終了コード: 0 ／1 失敗／2 対象が 2 件未満（突き合わせる相手がいない）。

## 決めたこと

記録日・理由・根拠は `docs/decisions.md` にあった当時の記録のまま。

### `braindex answer` の Markdown 変換は依存を足さず自前パーサで書き、決定性テストで担保する

記録日: 2026-09-03
理由: 原型の `md_to_html.py` が自前パーサ・純粋関数だったので、そのまま Go に写せば `go.mod` の依存ゼロを保てる。Markdown ライブラリを足すと `go install` の取得物が増え、版によって出力が揺れて「同じ入力から同じ HTML」の決定性を自分たちで担保できなくなる。対応する構文は見出し・表・リスト・チェックボックス・引用・コード・水平線・段落と行内の強調・コード・リンクに限る。却下: 外部の Markdown ライブラリ（依存と出力の揺れ）。
根拠: `work/SPEC-phase7.md` 設計 1／実装 `internal/mdhtml`（`TestPage_Deterministic`・golden テスト。PR #3（控え: pr/3.md）8（控え: pr/38.md））／`go.mod` に require が無いこと（2026-09-05 確認）

### `braindex scope` の索引の読み手は `internal/review.ParseCatalog` を再利用する

記録日: 2026-09-03
理由: catalog.md の形式（H2＝リポ・5 列の表）を 2 か所で定義すると、render を変えたときに片方だけ壊れる。review の読み手は 5 列でない行をエラーにするので、形式変更時に scope のテストも落ちて気づける。
根拠: 当時の `work/SPEC-phase6.md` §2（2026-09-03・削除済み）／`internal/review/catalog.go`／実装 `internal/scope/scope.go`（PR #4（控え: pr/4.md）3（控え: pr/43.md））

### `braindex scope -dir` の出力パスは、渡したディレクトリと結合した形にする

記録日: 2026-09-04
理由: 索引モードは `root` 相対で出るのに `-dir` だけ渡したディレクトリからの相対で出ており、受け取った側（矛盾検査のサブエージェント）がそのまま開けないパスを踏んでいた。同じコマンドの出力でモードによって基準が変わるのを避け、どちらも「そのまま開ける」に揃える。原型 `contradiction-scan/scope.py` の挙動でもある。却下: 現状維持（README と SKILL.md の但し書きで運用は回るが、但し書きを読まない受け手が踏み続ける）
根拠: 会話 2026-09-04（ユーザー判断・`work/APPROVALS.md` 1 の回答 A）／実測 2026-09-03: hub から `braindex scope -dir ../repo-x/docs/notes` の出力が `a.md` → `docs/notes/project/review-evidence-2026-09.md`「`scope -dir` の出力パスは渡したディレクトリからの相対だった」（出典は当時の PR #4（控え: pr/4.md）3（控え: pr/43.md） のレビュー記録 3 節・除去済み）

### `braindex scope -dir` の列挙は索引と同じ走査規則にする（`archive` セグメントとドットで始まるディレクトリを除く）

記録日: 2026-09-04
理由: 意図的に `archive/` へ退避したノートが矛盾検査の対象に混ざると「陳腐化」の指摘が出続け、`.git` 配下の `.md`（PR テンプレなど）も検査対象に入っていた。README の公開規約は索引についてこの 2 つを除くと書いており、`-dir` だけ違う理由が公開文書から読めない。同じフォルダ規約を 2 通りに読ませない。ただし起点として直接渡されたディレクトリ自身には掛けない（掛けると `archive/` を指定したときに全件消える）。`archive` の扱いは索引と同じだが、ドットで始まるディレクトリの除外は索引より広い——索引が `.` を見るのは `root` 直下のリポ選びだけなので、`docs/notes/.drafts/` のような置き場の中の隠しディレクトリは索引には載り、`-dir` では出ない（2026-09-04 実測: 同じ置き場で索引 2 件・`-dir` 1 件）。却下: 現状維持（`-dir` は索引を使わない逃げ道なので全部見せる。原型の `rglob` も同じだが、退避の意図を無視する欠点が上回る）
根拠: 会話 2026-09-04（ユーザー判断・`work/APPROVALS.md` 2 の回答 A）／実測 2026-09-03: 同じ置き場で索引 2 件・`-dir` 3 件 → `docs/notes/project/review-evidence-2026-09.md`「`scope -dir` の列挙が索引の走査規則と揃っていなかった」（出典は当時の PR #4（控え: pr/4.md）3（控え: pr/43.md） のレビュー記録 3 節・除去済み）／実装の前例は `internal/scan/scan.go` の `SkipDir`

### `braindex answer` のタブ更新は常駐サーバを持たず、HTML 内の JS タイマーによる自己リロードで行う

記録日: 2026-09-05
理由: 回答のスレッド化（追記したら開いているタブに出す）には更新手段が要るが、常駐サーバは 2026-09-03「持ち込まない（利用者にプロセスとポートの管理を負わせない）」で却下済みで、原型の常駐は再起動で消えて全タブがつながらなくなる穴も持つ。file:// のまま JS タイマーで自分を読み直せばプロセス・ポート・ログオン時の登録が要らない。代償は盲目的な読み直し（file:// からは更新有無を調べられない）によるちらつきと画面状態の消失で、スクロール・開閉状態の保存と復元、非表示タブと文字選択中の抑止、一時ファイルへ書いて改名、で抑える。入力欄を持つ approvals フォームには適用しない。ちらつきが実測で耐えられなければ「自動更新なし（新タブ）」へ戻す。却下: 常駐サーバを `braindex answer serve` として入れる（2026-09-03 を覆し、OS 別の自動起動登録まで抱える）／自動更新なし（原型で 2026-08-16 に直したタブ増殖が戻る）／`<meta http-equiv="refresh">`（条件を付けられない）
根拠: 会話 2026-09-05（ユーザー判断・承認フォームの回答 A。「自己リロードの問題」への回答は当時の `work/APPROVALS.md` 選択肢 A 本文）／原型の常駐が再起動で消える実測（同日: OS 起動 11:08:28、11:12 に別セッションで `/ping` 接続拒否・手動起動）／欠点の緩和策は実装時に実測（2026-09-05 時点は推測）／設計は `work/ISSUE-answer-thread.md`

### 承認フォームの送信成功は、フォームを畳んで完了パネルで示す（回答は応答を返す前にディスクへ書く）

記録日: 2026-09-06
理由: 送信成功の合図がフォーム全体を `opacity:.5` にして 13.5px の文を添えるだけで、押した人には「白くなった」としか見えず、
受理されたのか遅れているのか分からなかった。畳んで大きな完了パネルとタブ題名（✓ 送信済み）に変えれば視線の先が変わる。
あわせて回答 JSON は `/reply` が 200 を返す前に書き、保存先をパネルに出す——後から呼び出し側が書く形だと、書けなかったときに
ブラウザは完了表示のまま回答だけ消える。却下: 送信中のスピナー等の演出（受け口は 127.0.0.1 で待ち時間は実質なく、問題は成功表示の弱さ）／
`-apply` の結果まで画面に出す（受け口を反映の完了まで生かすことになり、2026-09-05「常駐しない・回答 1 回で終わる」と衝突する）
根拠: 会話 2026-09-05（ユーザー報告「送信を押すと白くなるだけで送れたか分からない」）／旧実装 `internal/approvals/form.go` の CSS `body.sent .ap{opacity:.5}`／実装は PR `approvals/sent-state`

### 回答の HTML は `braindex answer -append <話題>` で 1 つの話題に重ねる（追記は上・質問を見出しに・既読は畳む） → **既読の自動判定は同日中に撤回**（下の「開閉の操作をそのまま覚える」）

記録日: 2026-09-06
理由: この 3 つは同じ機能の 3 面で、揃って初めて「どこまで読んだか」が画面から分かる。エントリを不変にすれば
「新しいエントリだけが開いている」ことが差分表示の代わりになる。却下: 差分表示（前回との差を色で出す。エントリが不変なら不要）／
サーバ側で既読を持つ（常駐が要る）／`-q` の要約（ユーザーの言葉が変わると見出しで探せなくなる）
根拠: 会話 2026-09-05（ユーザー要望「追記を上に書くようにして欲しい」「ユーザーのこの質問に対する回答みたいな感じで」
「見たら折りたたまれて、追記したら開く」）／実装 `internal/mdhtml/thread.go`・`cmd/braindex/cmd_answer.go`

### スレッドのエントリの境界は HTML コメントのマーカー（`<!--braindex:entry at=… q=…-->`）で持つ

記録日: 2026-09-06
理由: 見出し（`## 日時 — 質問`）を境界にすると、回答本文に `##` があるだけでエントリが割れる。回答は見出しを含むのが普通なので、
本文に現れない記法で境界を持つ。属性なので日時と質問を機械で読み書きでき、`.md` のまま読んでも邪魔にならない。
却下: `---` の水平線（本文でも使う）／見出しレベルを 1 段下げて退避（本文の構造を書き換えることになる）
根拠: 実装とテスト `internal/mdhtml/thread_test.go`（`TestParseThread`: 本文の `## 内訳` でエントリが割れないこと）

### スレッド HTML の表示更新は常駐サーバを置かず、埋め込み JS の 12 秒タイマーで自分を読み直す

記録日: 2026-09-06
理由: 同じ話題の続きを出すたびに新しいタブが増えるのを避けたいが、常駐サーバは「常駐しない・外部送信なし」という
answer の前提を崩す。隠れているタブ・文字を選択中は読み直さず、スクロール位置と開閉を戻し、右上のボタンで止められる
ようにして、ちらつきの実害を抑える。入力欄を持つ HTML（approvals）には入れない。却下: 常駐サーバ＋SSE（原型の方式。
2026-09-05 に退役させたばかり）／自動更新なし（新タブが増える元の問題が残る）
根拠: 会話 2026-09-05（ユーザー判断・案 A）／実装 `internal/mdhtml/page.go` の `threadJS`／ブラウザでのちらつきは未確認（2026-09-06）

### スレッドのエントリは既読を自動で判定せず、開閉の操作をそのまま覚える（同日の「見たら畳む」を上書き）

記録日: 2026-09-06
理由: 「画面に 3 秒以上入ったら既読」と 12 秒の自己リロードが噛み合い、読んでいる最中にエントリが畳まれた。
既読という推定を持たず、畳んだ・開いたという操作だけを覚えれば、画面が本人の操作なしに変わることがなくなる。
読み終わったものを自分で畳めば結果は同じで、判定の当たり外れだけが消える。「新着」は一度も開閉していないエントリの印に変わる。
却下: 既読までの秒数を延ばす（当たり外れが残る）／自己リロードを止める（新しい回答が出たときに気づけない）
根拠: 会話 2026-09-06（ユーザー報告「いまだと読んでる最中に閉じられてうざい」「既読かどうかはユーザーが選択できるようにして」）／
実装 `internal/mdhtml/page.go` の `threadJS`（`ans-open:<ファイル名>:<エントリ id>`）

### `braindex answer` のローカル画像は `file://` で参照する。data URI の埋め込みは入れない

記録日: 2026-09-06
理由: 回答 HTML は「その場で読む一時物」（決定 2026-08-22）なので、軽いままにする。埋め込みは 1 MB の PNG が 12 枚で 16 MB 級になり、一時置き場の掃除まで重くなる。代償は承知のうえで、画像を移動・削除すると表示が消えること、HTML を他人に渡しても画像が付いていかないこと。却下: `-embed` を足して指定したときだけ埋め込む案（自己完結した 1 ファイルになるが、渡す場面が実際に出てから足せばよい）／既定で埋め込む案（一時物という位置づけと合わない）。
根拠: 会話 2026-09-06（ユーザー判断・承認フォームの回答。Claude の案は `-embed` を足す B だった）／一時物という位置づけは `~/.claude/rules/html-answers.md` と決定 2026-08-22／現象と原因は `work/ISSUE-bug-answer-images.md`

### フックが開いたフォームの回答は、braindex approvals wait で待ち、届いたらアシスタントを起こす

記録日: 2026-09-11
理由: フックはフォームを切り離して起動するので、回答が届いてもアシスタントに知らせる経路が無く、利用者がチャットに書き込むまで作業が止まるため（2026-09-11 に 2 回発生）。回答が届いたら終わるコマンドを braindex に足し、フックがアシスタントに返す指示文でバックグラウンド起動させる。起こす役をアシスタントの記憶に頼らず、フォームを開く仕組みと同じ場所に置ける。2026-09-10 までの形（アシスタントが自分でフォームを起動し、終了の通知を受ける）は、フォームが 2 枚開く問題で 2026-09-11 にやめたので戻さない。却下: 規則でアシスタントに見張らせる（仕掛け忘れると止まる。同じ回避策は 2026-09-11 1:28 に gameStudio のメモリにだけ書かれ、braindex のセッションでは効かなかった）／今のまま（回答のたびにチャットで一言書く必要がある）
根拠: 会話 2026-09-11（ユーザー判断・承認フォームの回答 2026-09-11T17:27:22+09:00）／原因の調査は docs/notes/project/approvals-answer-does-not-wake-claude-2026-09-11.md

### 解説コマンド `braindex explain <md>` は、私が書いた解説の md を読みやすい HTML にする部分だけを担う

記録日: 2026-09-12
理由: 解説の文章と図は私（Claude）が題材に合わせて書き、コマンドは目次・図・表からのグラフ・用語の定義を
備えた HTML を出すことに絞る。LLM を呼ばないので同じ入力からは同じ出力になり、決定性テストを書ける。
索引に LLM を入れない設計（2026-08-07）とも揃う。代償として、解説の質はコマンドでは保証されず、
私の書き方しだいになる。却下: B. `braindex explain <URL または PDF>` として取得から解説の下書きまで通す
（手数は最小だが、`news` と同じ opt-in の claude-cli 呼び出しが要り、出力が毎回変わる。外部サイトへの取得と
PDF の読み取りも増える。取得と要約は私が今やっていることの置き換えで、質は上がらないのに壊れる箇所が増える）
／C. コマンドを作らず書き方の手順（スキル）だけ決める（実装は要らないが、図解の手動コピーと 2 ファイルへの
分裂が残り、md に書いた SVG が表示されない問題も直らない）
根拠: 会話 2026-09-12（ユーザー判断・承認フォームの回答 2026-09-12T15:18:06+09:00）／今の 2 ファイル運用の実例は
brain の STAIR_目次ベース検索_2026-09-12.md と トレースエンジニアリング_エージェント可観測性_2026-09-12.md
／SVG が文字として出る実測は work/ISSUE-explain-command.md

### 解説の図は別の `.svg` ファイルに置き、md からは `![図1](fig1.svg)` で参照する

記録日: 2026-09-12
理由: ノートは後のセッションが索引から引いて全文を読み返すものなので、本文が図の記述で埋まると読む手間が増える。
実測では図 1 枚が約 4KB で、6 枚の図解が 26KB、解説の本文が 15KB だった（brain の STAIR 図解・2026-09-12）。
md に直接書くと本文が 3 倍近くにふくらむ。出力される HTML は 3 案とも同じになるので、差は md の読みやすさにだけ出る。
`explain` は参照先の `.svg` の中身を HTML に埋め込み、1 枚で完結させる。代償は、図の数だけファイルが増えること
（解説 1 本で 5 枚なら 6 ファイル）と、md に直接書いた生の `<svg>` は今までどおり文字として出ること。
却下: A. `<svg>` から `</svg>` までをそのまま通す（今の図解 HTML の書き方を持ち込めるが、ノートの本文がふくらむ。
`<script>` と `on...` 属性を落とす検査も要る）／B. ``` の囲みに `svg` と書く（md の形は崩れないが、
本文がふくらむ点は A と同じで、GitHub では図でなくコードとして表示される）
根拠: 会話 2026-09-12（ユーザー判断・承認フォームの回答 2026-09-12T16:12:53+09:00）／図と本文の大きさは
brain/docs/notes/STAIR_目次ベース検索_図解_2026-09-12.html（SVG 6 枚・26KB）と同名の .md（15KB）の実測
／相対パスの解決はコミット 4a5732b で先に直した

### `explain` の数値のグラフは、md の表から機械が描く（手書きの .svg にしない）

記録日: 2026-09-12
理由: 手で描くと棒の長さを目分量で決めることになり、グラフと本文の数値が食い違っても読み返して気づけない。
表の数値から目盛りと長さを計算すれば、その食い違いは構造的に起きない。同じ入力からは同じ絵になるので
決定性テストで確かめられる。元の表もグラフと一緒に残し、数字そのものを読めるようにする。
描けるのは棒と折れ線の 2 種類に絞る。概念図は決定 2026-09-12 のとおり手書きの `.svg` のままなので、
表現の幅は狭まらない。却下: B. 図と同じく手で `.svg` に描く（実装は要らないが、数値と絵の食い違いを
誰も検出できない）／C. B で始めて食い違いが起きたら A に移る（最初は軽いが、気づけない誤りを
「起きたら直す」前提に置くことになり、切り替えの手間も残る）
根拠: 会話 2026-09-12（ユーザー判断・承認フォームの回答 2026-09-12T16:15:29+09:00）／
実装と書式は PR #4（`internal/explain/graph.go`）と `manual/tools.md` の「数値のグラフ（表から描く）」節。
当時の仕様は work/SPEC-explain-command.md にあったが、実装の完了時に消した

### `explain` の `y=` を省いたときは、空でないセルの過半数が数値として読める列を描く

記録日: 2026-09-12
理由: 表には「-」や「n/a」の欠測が混じるので、「全セルが数値の列だけ」にすると、欠測が 1 つあるだけで
その系列が黙って消える。逆に「数値が 1 つでもあれば描く」にすると、備考の列に数字が 1 つ紛れただけで
意味のない系列が生える。過半数なら、どちらも起きない。見分け方が 1 行で言えるので、描かれなかったときに
理由を追える。却下: 全セルが数値の列だけ（欠測に弱い）／数値が 1 つでもある列（文字の列を拾う）。
どちらを採っても `y=` を明示すれば上書きできるので、既定の選び方だけの違いになる。
根拠: 実装 2026-09-12（PR #4・`internal/explain/graph.go` の `isNumericCol`・
テスト `TestGraph_数値列の見分け`）／書式は `manual/tools.md`。
当時の仕様（work/SPEC-explain-command.md）は「x 以外の数値列すべて」とだけ決めていて、
数値列の見分け方を書いていなかったので、ここで決めた

### 図の色は explain のテンプレート側に 1 か所だけ置き、図の側には控えを書かない

記録日: 2026-09-12
理由: 図と本文で配色が食い違う事故の原因は、色の値を図ごとに書き写していたこと。控えを図へ戻す案
（単体表示のときだけ効く 1 ブロックを置く／使うたびに `var(--fg,#1c2230)` と書く）は同じ複製の
余地を残すので採らない。単体で開いた図が黒く潰れる代わりに、確認は `braindex explain` で本文に入れて見る。
根拠: 実装とテストは `internal/explain/style.go` の `figCSS` と `figpalette_test.go`（コミット `adf4320`）／
実測 2026-09-12: 埋め込むと図の文字は本文と同じ `rgb(28,34,48)`・背景との比 14.83、暗い配色では
`rgb(232,235,241)`・比 15.31。単体で開くと `fill="var(--fg)"` も `fill="var(--s1)"` も黒、
`stroke="var(--c1)"` は `none` に落ちる／会話 2026-09-12（ユーザー判断・承認フォームの回答）

### 図の系列色は本文の棒グラフと分け、文字にも使える 4 色にする

記録日: 2026-09-12
理由: 図では系列の色を短い見出しの文字に使うので下限が 4.5 になるが、棒グラフは塗りなので 3.0 で足りる。
用途が違うため、同じ値を流用すると片方が必ず割れる。5 色のまま濃くする案は採らない——青と赤紫を
どちらも 4.5 まで濃くすると、色覚の型によっては見分けにくくなる（隔たり 30・明るさ比 1.26。
基準は 38 または 1.5）。色つきの文字をやめる案も採らない。色で群を示している図が壊れる。
根拠: 実測 2026-09-12——図の 4 色（青 `#0067A0`・橙 `#BB5300`・緑 `#007656`・灰 `#3B4047`）は
背景との比 4.50〜9.75 で、6 ペアすべてが見分けの基準を満たす。5 色を流用していたときは
`#D55E00` が 3.61、`#C86D9F` が 3.19 だった／棒グラフの色は決定「配色の直し #5」のまま変えない／
会話 2026-09-12（ユーザー判断・承認フォームの回答）

### 承認フォームの開き忘れは `approvals hook` で機械的に拾う

記録日: 2026-09-09
理由: 判断待ちを積んだのにフォームを開かず、文章で「承認待ちが残っています」と書くだけで終える取りこぼしが起きた。規約の文が出力時に効かない型の失敗なので、機械で担保する。利用側が同じ処理を自前スクリプトで書いていたが、道具の正本は braindex なのでこちらへ寄せた。同じ内容の判断待ちでは一度だけ開き（内容ハッシュで重複を抑止）、終了コードは常に 0 にして会話を止めない。
根拠: 実装は `cmd/braindex/cmd_approvals_hook.go`（初出コミット `75b3073`「approvals: 開き忘れを拾う hook サブコマンドを足す」）と OS 差の切り離し起動 `cmd_approvals_hook_detach_windows.go` / `_unix.go`／テスト 7 件（`cmd_approvals_hook_test.go`。重複抑止と空判定は実装を殺して Red を確認）／手順は `manual/tools.md`「開き忘れを拾う（`hook`）」／利用側の報告は会話 2026-09-09

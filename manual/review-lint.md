# braindex review / lint — 週次レビューと検査

[← README](../README.md) ／ [手引きの目次](README.md)

## braindex review — 週次レビューの下書き

hub で `braindex review` を実行すると、`work/review/<今日>.md` に週次レビューの下書きができる。集計は CLI が規則ベースで行い、
判断（差分の要約・アーカイブの可否・次アクション）は人か、人が使うエージェント（hub に入るスキル `braindex-review`）が埋める。
索引 `index/catalog.md` は読むだけで書き換えない（再生成は `braindex`）。

下書きの節（この順・固定）:

| 節 | 中身 | 埋めるのは |
|---|---|---|
| 索引（件数と増減） | 前回レビュー時点の索引（hub が git 管理下ならそのコミット、無ければディスクの索引）と、いま走査した結果の差。リポ別に 追加／変更（変わった列名つき）／削除。前回にあって今回無い行のうち、今回読めなかった範囲にあるものは「確認不能」、いまの設定では走査しない場所にあるものは「対象外」に分け、削除に数えない。節の頭に確認不能・対象外の数（0 なら出さない）と、今回・前回の走査の記録（読めなかった範囲の一覧） | CLI |
| 差分ファイル（リポ別） | 各リポで前回以降のコミットが `notes_dirs` と `docs/decisions.md` に触れたファイル（`git log --since --name-status`）。git 管理外のリポは飛ばして警告 | CLI |
| 放置 TODO | 各リポの `work/TODO.md` の未完了項目のうち、行が最後に変わった日（`git blame`）が `stale_todo_weeks` 週より前のもの。git で追えなければ mtime に `~` | CLI |
| アーカイブ候補（機械条件のみ） | `archive_months` か月より前で、今回の差分に無いノート。`decisions` は含めない。読めなかった範囲のノートは候補に入れず、判断もしない（節にその旨を断る） | CLI |
| 今週の差分ダイジェスト／アーカイブ（実施・見送りと理由）／次アクション | 見出しだけ | 人 |

**前回の判断の節が空のままなら伝える**（2026-09-06 追加）。前回の下書き `work/review/<前回日>.md` を読み、
上の 3 節が見出しとひな型の行だけなら、今回の「次アクション」の直下に
`- （braindex）前回（<前回日>）の判断の節が空のまま: <節名>` を書き、
終了コード 2 にする。機械節は毎週埋まるので回っているように見えるが、判断の節が空なら振り返りの回路は動いていない。
見出しごと無いファイル（手で書いた覚え書きなど）には言わない。
頭の `（braindex）` は braindex 自身が書いた行の印で、翌週の判定ではこの行を人の記入と数えない
（数えると、誰も埋めていないのに翌週から警告が出なくなる）。

フラグ: `-config`（設定ファイル＝hub の位置。既定はカレントの `braindex.json`。無ければ失敗）`-date YYYY-MM-DD`（今日の固定）`-since YYYY-MM-DD`（前回日。既定は記録の置き場にある最新の `YYYY-MM-DD.md`、無ければ `since_days` 日前）
`-out`（出力先。既にあれば書かない）`-stdout`（標準出力へ）。
終了コード: 0 成功／1 失敗（フラグの誤り・設定が無い・出力先が既にある。何も書かない）／2 警告つき完了（git 不在・git 管理外のリポを飛ばした）。
git はあれば使う。無い環境でも索引の増減（ディスクの索引との比較）・放置 TODO（日付は mtime で `~` つき）・アーカイブ候補は出る。

## braindex lint — ISSUE とノートの検査

`work/ISSUE-<slug>.md`（作業状態。テンプレ `docs/conventions.md` の形）が規約どおりか、ノート（それ以外の `.md`）が
曖昧でないかを規則ベースで検査する。索引には載せない。`ISSUE-*.md` は形の検査、それ以外はノートの検査になる（`-kind issue|note` で固定できる）。

```sh
braindex lint                         # 各リポ(root の repo_depth 段下・既定は直下)の work/ISSUE-*.md をまとめて検査(root は索引と同じ解決規則)
braindex lint work                    # ディレクトリ直下の ISSUE-*.md
braindex lint work/ISSUE-x.md         # ファイル
braindex lint docs/notes/x.md         # ノートの曖昧さ検査(用語集は同じリポの docs/glossary.md を自動で探す)
braindex lint -kind note docs/notes   # ディレクトリ直下の *.md をノートとして検査
```

指摘は stdout に `パス:行: [種別] 内容` で出す（ファイル全体に掛かる指摘は行番号なし。`-json` なら `path`・`line`・`msg`・`kind`・`severity` の配列）。
終了コード: 0 指摘なし／1 失敗（フラグ・root・パスの誤り）／2 指摘あり。

### ISSUE の形

| 検査 | 指摘 |
|---|---|
| 見出し | 先頭の見出しが `# ISSUE:` で始まらない |
| 必須の節 | `## 現在の作業` `## 状態` が無い（対象リスト・メモは任意） |
| 現在地 | `← いまここ` が無い、または 2 つ以上 |
| 最終更新 | `最終更新: YYYY-MM-DD` が無い・形式が違う・未来・`-stale-days N` 以上たっている |
| 仕様 | `仕様: SPEC-<slug>.md` の参照先が同じディレクトリに無い |
| HEAD 比較（git 管理下のみ） | HEAD にあったチェック項目（`- [ ]`／`- [x]`）が消えた／内容が変わったのに最終更新が HEAD と同じ |

フラグ: `-config` `-root` `-date YYYY-MM-DD`（基準日）`-stale-days N`（0 で見ない）`-no-git`（HEAD 比較をしない）
`-kind issue|note` `-glossary <用語集>` `-json`。
git が無い・git 管理外のファイルは HEAD 比較を飛ばす（要約の「HEAD 比較 N 件」で分かる）。

なぜ: ISSUE は「次のセッションが 1 枚読んで再開できる状態」を目的にするが、更新のたびに丸ごと書き直すので、既存の項目を落とす事故が起きる。
エージェントの実行状態をランタイムが検証して不正なら差し戻す設計（SKILL.state, arXiv:2608.26263）と同じ形で、機械が形を確かめ、判断は人がする。

### ノートの曖昧さ

後から読む者（人もエージェントも）が事実をもっともらしく再構成してしまう書き方を拾う。確度は 2 段階で、warn はほぼそのまま直してよく、
candidate（種別名に「(候補)」が付く）は本文の意味で真偽を確かめてから直す。判断と修正案の手順は hub に入るスキル `record-lint`。

| 種別 | 確度 | 指摘 |
|---|---|---|
| 曖昧な数量詞 | warn | 多い・最近・かなり など、数値や日付に置き換えるべき語 |
| 日付なし | warn | 本文に日付（`YYYY-MM-DD`・`YYYY/M/D`・`YYYY 年 M 月`）が一つも無く、ファイル名（`YYYYMMDD`・`YYYY-MM-DD`）にも無い |
| 出典なき数字 | candidate | 単位つきの数や小数がある行に、出典マーカー（出典・根拠・参照・実測・→・URL・ファイル名 など）が無い |
| 裸のヘッジ | candidate | たぶん・かもしれない・〜と思う などが、（推測）・未確認 のタグも出典も無いまま付いている |
| なぜ欠落 | candidate | 決定（`記録日`・`採用日` の語を持つ `##` ブロック。`decisions.md` は全ブロック）に理由の語が無い |
| 根拠欠落 | candidate | 決定に `根拠:` 行が無い |
| 失効行の書式・日付・後継 | warn | `decisions.md` の `## ` 見出し直下で「失効」「一部失効」で始まる行を検査する。書式は `失効: YYYY-MM-DD → 後継見出し` または `一部失効: YYYY-MM-DD → 後継見出し（範囲）`。日付が実在し、後継名で始まる `## ` 見出しが同じファイルにあること（前方一致）。旧表記と失効行の無い見出しは対象外 |
| 未定義用語 | candidate | 「鉤括弧の語」・`[[link]]`・英大文字始まりの語が用語集に無い（`-glossary` か、同じリポの `docs/glossary.md` があるときだけ） |

コードフェンスの中は見ない（未定義用語だけは本文全体から語を拾う）。表の行は出典なき数字と裸のヘッジで見ない。語彙表は日本語のみで CLI に埋め込む。

## 決めたこと

記録日・理由・根拠は `docs/decisions.md` にあった当時の記録のまま。

### `braindex lint` で `work/ISSUE-*.md` を決定論で検査する。索引には載せない

記録日: 2026-09-02
理由: 「1 処理単位ごとに即更新」「次のセッションが 1 枚で再開できる」は規約にあるが、守られているかを確かめる仕組みが無かった。論文 SKILL.state は状態パッチをランタイムが検証して不正なら差し戻す。同じ形で、必須の節・「← いまここ」1 つ・最終更新・SPEC 参照先の存在・（git 管理下なら）チェック項目の消失と最終更新の据え置きを warning に出す。索引に載せる案は「work/ は索引しない」（2026-08-07）に反するため却下。record-lint（曖昧さの検査）とは担当が違う。git は有れば使い、無ければ HEAD 比較を飛ばす（依存を増やさない）。
根拠: 会話 2026-09-02（ユーザー判断）／同上ノート（具体案 c）

### 週次レビューの機械部分を `braindex review` に持たせ、下書きの節の順を固定する

記録日: 2026-09-02
理由: `docs/conventions.md` 週次レビュー節の「braindex が担うのは機械部分」を実装した。テンプレの skill に bash 手順（`braindex`・`git diff --stat`・`git log --since`）として散っていた集計を 1 コマンドにし、skill は差分の実物を読んで判断の節を埋めるだけにする。下書きは 7 節固定（索引の増減／差分ファイル／放置 TODO／アーカイブ候補は CLI が埋め、ダイジェスト／アーカイブ／次アクションは見出しだけ）。索引は読むだけで書き換えない。skill の frontmatter から `model:` を外した（モデル名に依存させない）。
根拠: 当時の `work/SPEC-phase2.md`（2026-09-02・2026-09-03 に削除済み）／実装 `internal/review/`・`cmd/braindex/cmd_review.go`（PR #17（控え: pr/17.md）〜#2（控え: pr/17.md〜pr/2.md）2（控え: pr/17.md〜pr/22.md））／`docs/conventions.md` 週次レビュー節（v0 の「1 と 2 の機械部分」）

### `braindex review` の「前回の索引」は hub の git から前回日時点のコミットを取る（無ければディスク → 無し）

記録日: 2026-09-02
理由: テンプレの CLAUDE.md は「書いたら `braindex` を実行してコミット」を推奨するので、レビュー時点のディスク上の索引は週の途中で更新済み。ディスクの索引としか比べないと索引の節が毎回「差分なし」になる。計画（`ISSUE-phase2.md` 2026-09-02 の「git 不要」）を上書き。差分の計算自体は git に依らない純関数で、決定性テストはそこに掛ける。hub が git 管理外・前回日以前のコミット無しならディスク、それも無ければ空（初回は全件が追加）。
根拠: 会話 2026-09-02（ユーザー指示「phase2対応して」を受けた自走中の私の判断。ユーザー未確認）／実測: 作者の hub で 2026-09-02 に `review -stdout` を実行し、前回の索引を前回日以前の最終コミットから取れた（PR #1（控え: pr/1.md）8（控え: pr/18.md） 本文）

### 放置 TODO の日付は行単位（`git blame` の author-time）で見る

記録日: 2026-09-02
理由: 計画の「ファイルの最終更新（`git log -1`）」だと 1 項目足すたびに全項目が新しく見え、規約の「4 週間以上動いていない項目」を拾えない。git 管理外・未追跡はファイルの mtime を全行に当て、`~` を付けて区別する。
根拠: 会話 2026-09-02（自走中の私の判断。ユーザー未確認）／PR #1（控え: pr/1.md）9（控え: pr/19.md） のテスト（TODO を 2 回コミットし、古い行だけが残る）

### `braindex review` は出力先が既にあれば書かない（終了コード 1）

記録日: 2026-09-02
理由: skill や人が判断の節を埋めた後に同じ日にもう一度実行すると、上書きで判断が消える。共通ルール「手を入れられた成果物は上書きしない」の適用。標準出力に出す `-stdout` と別名の `-out` を逃げ道にする。
根拠: `~/.claude/CLAUDE.md` 会話の基本形 6（2026-08-31）／PR #2（控え: pr/2.md）0（控え: pr/20.md） のテスト

### `braindex review` の初回（前回の索引が無い）はアーカイブ候補を出さない

記録日: 2026-09-02
理由: 前回の索引が無いと全件が「追加」になり、「今回の差分に無いノート」が空になるため。初回から古いノートを候補にしたければ、索引をコミットしてから翌週に実行するか、`-since` で前回日を与える。既知の挙動として記録する（変えるなら「追加」を差分に数えない判断が要る）。
根拠: PR #2（控え: pr/2.md）0（控え: pr/20.md） の e2e テスト（init した空 hub で候補が `- なし`）

### `braindex review` の差分ファイルの状態（追加／変更／削除）は「前回日の時点にあったか × 今あるか」で決める → A. 実装のままにし、SPEC-phase2 の規則を上書きする

記録日: 2026-09-03
理由: 週次レビューで読者が知りたいのは「今読めるノート」と「前回から消えたノート」。前回に無く今ある＝追加／前回にあり今もある＝変更／前回にあり今は無い＝削除とし、窓の中で作って消したファイルは載せない。旧規則「削除（最新が D）＞追加（A が 1 度でもある）＞変更」だと、作ってすぐ消した下書きが毎週「削除」として載る。却下: B. 実装を SPEC に合わせ直す（後続の小 PR）。
根拠: 会話 2026-09-03（ユーザー判断。「AI が生成するノートの話か」の確認を経て A）。食い違いの発見は当時の PR #1（控え: pr/1.md）8（控え: pr/18.md） のレビュー記録 3-5（2026-09-03・git 管理外・現存せず）。実装は `internal/review/git.go` の `parseNameStatus`、テストは `internal/review/git_test.go`。SPEC-phase2 の該当行は同日に上書き済み

### ノート検査の根拠行の遡及規則（記録日 2026-08-25 以降だけ必須）は持ち込まず、記録日を持つ決定は全件で `根拠:` 行を見る

記録日: 2026-09-03
理由: 原型 record-lint の遡及規則は作者の運用（2026-08-25 に 3 段を採用し既存エントリは触ったときに直す）に紐づく日付で、テンプレの `docs/conventions.md` は最初から 3 段を要求するため、利用者には既存の免除が無い。日付を埋め込むと利用者に意味の無い分岐が残る。
根拠: 当時の `work/SPEC-phase6.md` §1（2026-09-03・削除済み）／原型 `~/.claude/skills/record-lint/lint.py` の `EVIDENCE_RULE_SINCE`（2026-09-03 に読んだ）／実装 `internal/lint/note.go`（PR #4（控え: pr/4.md）1（控え: pr/41.md））

### ノート検査の出典なき数字は原型の単位語をそのまま使い、「本文」の「本」のような過検知は skill の意味判断で落とす

記録日: 2026-09-03
理由: 単位語（本・件・人・回 など）は日本語の一般語と重なり、機械で完全には切り分けられない。candidate は本文の意味で真偽を確かめる設計なので、語彙を絞って取りこぼすより、拾って落とす側に倒す。
根拠: 実装中の実測（`2026-09-01 本文。` が出典なき数字の候補になった。2026-09-03・`cmd/braindex/cmd_lint_test.go` の例文を変えて回避）／設計判断 2026-09-03「決定論部分＝CLI、意味判断＝skill」（本ファイル 299 行目）

### `braindex review` の差分ファイルの起点は「前回の索引を取ったコミットの時刻」にする

記録日: 2026-09-06
理由: 前回の索引はその日の最後のコミット（`--until=<前回日> 23:59:59`）から取るのに、差分ファイルは前回日の 0 時から数えていた。同じ日のうち索引を取る前に入った変更が、前回の索引にも今回の差分にも出て二重に数えられる。前回の索引がコミットから取れなかったとき（初回・git 管理外）だけ、従来どおり前回日の 0 時にする。`git log --since` はその時刻ちょうどのコミットを含むので、境界の 1 コミットは二重に出るが、見落とすよりは害が小さいのでそのままにする。日付だけを渡したときは 0 時を補う（git は日付だけの `--since` を「その日の実行時刻」と解釈するので、時刻を書かないと結果が実行時刻で変わる）。
根拠: 設計レビュー 2026-09-06 M3(b)／`--since` の境界と走査の打ち切りは実測（2026-09-06・git 2.39.2 → `docs/notes/common/git-since-boundary.md`）／実装は `internal/review/git.go` と `internal/review/review.go`（PR `review/m3b-tz-windows`）

### `braindex review` は前回の索引を読めなくても下書きを出す（増減の代わりに理由を 1 行）

記録日: 2026-09-06
理由: 前回の索引の解析に失敗すると全体をエラーで止めていたが、読めない理由は「前回の版が索引の形式より古い」「手で壊した」で、こちらのバグではない。増減が出せないだけで、差分ファイル・放置 TODO・アーカイブ候補は作れるので、週次レビューの下書きは出したほうが役に立つ。件数を 0 として増減を出す案は却下——「前回 0 件 → 今回 N 件（追加 N）」になり、全件が新しくなったように読める。今回の索引が読めないのはこちらのバグなので、従来どおりエラーで止める。
根拠: 設計レビュー 2026-09-06 M3(c)（`docs/notes/project/design-review-2026-09-06.md`）／実装は `internal/review/review.go` と `WriteIndexUnavailable`、判定は `TestReview_前回の索引が読めなくても続ける`（PR `review/m3c-order-degrade`）

### 判断の層が動いたかを機械で見る: `review` は前回の判断の節が空なら伝え、`retro check` は所見ノートが無ければ添える

記録日: 2026-09-06
理由: 機械節（索引の増減・差分ファイル・放置 TODO・訂正率）は毎週埋まるので、回路が回っているように見える。しかし判断の層（差分の要約・アーカイブの可否・次アクション・振り返りの所見）が空のままだと、材料を作っているだけで何も決めていない。それを検出するのに新しい状態ファイルは作らない——「振り返ったか」の答えはノートや下書きの中身そのもので、別に記録すると食い違う。`review` は前回の下書きの節 5〜7 が見出しとひな型だけなら今回の「次アクション」の直下に 1 行書いて終了コード 2、`retro check` は鳴らしたときだけ `docs/notes/retro-<窓の中の日付>.md` の有無を見て 1 行に添える。見出しごと無い下書き・`docs/notes/` ごと無い hub には言わない（その規約を採っていない人に、直しようのないことを毎回言わない）。
根拠: 設計レビュー 2026-09-06 M7（`docs/notes/project/design-review-2026-09-06.md`）／実装は `internal/review/judgement.go` と `cmd/braindex/retro_note.go`、判定は `TestEmptyJudgementSections`・`TestReview_前回の判断の節が空なら伝える`・`TestRetroCheck_所見ノートが無ければ添える`（PR `review/m7-loop-check`）

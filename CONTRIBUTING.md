# 貢献のしかた

小さく焦点の絞られた PR を歓迎します。このファイルには、索引の信頼性を保つために守ることだけを書きます。

## ビルドとテスト

```
go build ./...
go vet ./...
go test ./...
gofmt -l .      # 何も出力しなければ整形済み
```

Go 1.26 以降。依存は標準ライブラリのみで、CLI にサードパーティの依存を足さないでください。

golden ファイルは `internal/catalog/testdata/` にあります。出力を意図して変えたときは
`go test ./internal/catalog/ -update` で再生成し、同じコミットに含めてください。

## 守ること

- **決定性。** 同じ入力からは常にバイト一致の出力が出る（生成日の行だけが変わる）。走査規則や出力形式を変える
  ときは、決定性テストを同じコミットに含める（`TestBuild_Deterministic`・`TestBuild_DeterministicAcrossMtime`）。
- **索引に LLM を入れない。** 要旨は機械的に抽出する。これは設計判断（2026-08-07）であって、制約ではない。
- **意味検索・ベクトル DB を入れない**（設計判断 2026-08-19）。
- **CLI はノートを書き換えず、どこにも送信しない。**
- **個人識別子を入れない。** ユーザー名・絶対パス・実在の私用リポ名を、コード・testdata・文書に書かない。
  テストの fixture は架空の名前（`repo-a`・`ext`・`alpha`）を使う。

## コミットと PR

- 1 コミット 1 つの意味のある変更。機能とそのテストは同じコミット。バグ修正は落ちるテストから始める。
- PR は変更ファイル 20 前後まで。
- 設計判断は、結論 → 理由 → 根拠 の 3 段・日付つきで作者が記録する。PR の説明に判断とその理由を書いてください。
- 文書とコードのコメントは日本語。エラー・警告・索引の文言も日本語。

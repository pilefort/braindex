# braindex — このリポで作業するときの決まり

## 正本の所在

- 設計思想・設計判断・用語・フォルダ規約は `docs/`（`overview.md` `decisions.md` `glossary.md` `conventions.md`）にある。
  **`docs/` と `work/` も git 管理する**（2026-09-03 決定・旧「git 管理しない」2026-09-02 を上書き）。設計メモと作業記録も公開物なので、
  個人識別子（ユーザー名・絶対パス・実在リポ名）と作者の私的な題材を書かない
- 設計判断は `docs/decisions.md` に 結論 → 理由 → 根拠 の 3 段・記録日つきで書く。覆すときも消さず日付を添えて上書きする
- 用語は `docs/glossary.md`。定義だけを書き、実装詳細は書かない
- 索引の走査対象は `docs/conventions.md` のフォルダ規約に従う。規約の公開版は README と `braindex init` のテンプレ

## 作業の型

- 会話は日本語。結論を最初の 1 文で言う
- 作業状態は `work/ISSUE-<スラッグ>.md`（git 管理下・2026-09-03〜）。着手前に対象の全件リストを書いてから始め、完了したらファイルごと消す（履歴に残る）
- 未決の判断は `work/APPROVALS.md` に積み、勝手に決めない
- 索引の出力形式・走査規則を変えるときは、決定性テスト（同一入力で 2 回生成してバイト一致）を同じコミットに含める
- 機能とそのテストは同じコミット。バグ修正は再現テストを先に書く
- PR は変更ファイル 20 前後まで。機能単位で細かく分け、積み上げ式で出す

## してはいけないこと

- 索引生成に LLM を入れない（設計判断 2026-08-07「要旨は機械抽出」）
- 意味検索・ベクトル DB を入れない（同 2026-08-19）
- 作者の私用 brain の内容（ノート本文・索引・ネタ帳・レビュー）や、作者の個人的な題材（動画制作など）を持ち込まない。持ち込んでよいのは仕組みだけ
- 個人識別子（ユーザー名・絶対パス・実在リポ名）をコード・testdata・文書に書かない
- ノート本文を外部サービスへ送らない
